import torch
import torch.nn as nn
import torchvision.transforms

import PIL
from PIL import Image

def InferenceText():
    pass

# may not need this code , since we may deploy the model to Azures model repository/API. Where we can call the model from there.